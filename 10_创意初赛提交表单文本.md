# 附件八｜创意初赛提交表单文本

> 可直接复制到比赛表单；具体模型权重、参数、许可证和下载链接仅见附件七。

## 一、方案名称

**棱镜上新 PrismLaunch——证据驱动的跨境商品上新智能体**

## 二、方案概述

棱镜上新 PrismLaunch 面向有海外上新需求的国内品牌方、工贸商家和跨境运营人员，聚焦“国内中文商品资料 → 海外平台可发布商品资产”的主链路，首期支持 Amazon US 与 Shopify US。

国内商家常收到零散的 Excel、商品图、包装图和规格 PDF。资料缺失、重复商品、中文表达与海外使用习惯不一致、单位转换、平台字段差异和无证据宣传，会让“已有商品”难以稳定上架。项目将“商品资料输入”设计为独立的资料库维护流程：它持续维护商品表、图片和 PDF，按 SKU 完成字段校验、文件关联和重复识别；系统从文件中自动提取并预填商品信息，标明字段来源与置信状态，再按“基础搜索、售价计算、Listing／发布”分层提示缺口。基础搜索信息不足时仅保留资料草稿，不进入候选池；成本、原产国、申报价值、重量、包装尺寸、用途、材质等信息缺失时商品仍可基础搜索，但在后续售价计算时不输出建议售价；完全重复记录不入库。

业务主线从创建上新任务开始。运营人员通过固定条件选择目标平台、目标国家、中文一级商品大类和“最低单件利润 ≥ 金额”，再填写补充要求。大语言模型将要求整理为可审阅、可编辑的选品条件卡；检索和重排模块从私有候选商品池找到语义更匹配的 SKU，规则引擎基于硬条件、资料状态和重复状态给出分层与原因。商品入选后，系统完成多模态事实核验；每个新任务再独立获取并冻结汇率、费用和税费口径，在事实确认后反算可编辑建议售价，最后生成本地化 Listing、审核并输出 Shopify 草稿或 Amazon 上传文件。

方案包含六项连续能力：**资料输入与导入校验、重复识别与候选池管理、动态智能选品、商品事实核验、售价计算、受控 Listing 生成与平台审核发布。** 方案将“是否匹配上新需求”与“建议卖多少钱”明确分开：建议售价不参与检索、重排、P1／P2／P3 分层或推荐排序。模型不修改商品字段、不清除导入告警、不让重复记录入库，也不绕过规则直接发布。目标是在资料齐备时将首版商品草稿压缩至分钟级，同时降低重复发布、事实错误、本地化失真与无证据宣称风险。

## 三、核心功能

### 1. 资料输入与导入校验

- 输入 Excel／CSV、商品图、包装图、规格 PDF 等国内商品资料；
- 按 SKU 归集表格字段、图片与附件，校验格式、字段完整度和文件关联；
- 导入时自动从商品表、图片和 PDF 预填可识别字段，并展示来源与置信状态；
- 按基础搜索、售价计算、Listing／发布分层提示资料缺口；后续模块只读取既有状态，不在推荐阶段生成告警。

### 2. 重复识别与候选池管理

- 结合 SKU、条码、主图哈希、标题和关键属性识别重复；
- 完全重复记录仅保留导入日志，**不纳入候选商品库**；
- 疑似重复记录进入人工合并审核，合并后再决定是否进入候选池；
- 保证检索、排序、生成和发布均基于干净、可追溯的私有商品池。

### 3. 动态智能选品

- 运营人员用固定字段选择目标平台、目标国家、中文一级大类和最低单件利润；自然语言只补充颜色、规格、风格、场景与排除项；
- 大语言模型输入固定搜索条件、补充要求和候选字段说明，输出可审阅、可编辑的选品条件卡；
- 关键词检索与语义检索并行从私有候选池找回相关商品，融合后形成候选列表；
- Reranker 重排模型调整候选顺序，规则引擎读取硬条件、资料状态和重复状态，输出优先级分层、推荐原因与下一步动作；
- 最低单件利润和后续建议售价不参与选品排序。

### 4. 商品事实核验

- 多模态解析将表格、OCR、图片和 PDF 中的属性候选与证据位置绑定；
- 商品事实卡完成图片—商品资料交叉核对：将颜色、款式、可见配件、容量标识和图片文字与同一 SKU 的商品表、规格 PDF 比对；
- 图片无法单独证明的材质、认证、安全或性能不作为事实依据；冲突时转人工确认；
- 自动推算税则候选编码及置信度，仅用于后续进口关税预估，不形成自动报关结论。

### 5. 售价计算

- 生成选品条件后，后台创建“费用费率与汇率快照”：每个新任务重新获取最新可用参考汇率，同一任务内固定该汇率、平台费用版本和税费计算口径；
- 商品事实确认后，按已确认成本、物流／履约资料、税费预估、平台费用和最低单件利润反算建议售价；
- 缺少成本、币种、重量尺寸、原产国或归类要素时，不输出建议售价，直接定位待补字段；
- 建议售价写入 Listing 草稿的可编辑价格位置，运营人员可修改并保留版本；它不参与选品排序，不等同最终成交价或财务净利润。

### 6. 受控 Listing 生成、平台审核与发布反馈

- 只将事实卡中已确认、允许上架的事实和图片送入生成模块；
- 为 Amazon US 生成标题、五点、描述、搜索词和结构化字段，为 Shopify US 生成商品详情、SEO、标签和 Metafields；
- 检查类目、必填字段、单位、变体、风险词、证据覆盖和图片引用；最终审核只审核 Listing 草稿与平台规则，不重新判断商品图片本身；
- Shopify 创建可编辑商品草稿，Amazon 生成可复核的批量上传文件，回收错误码、修复动作和版本记录。

## 四、技术方案

### 1. 总体链路

```text
独立资料库流程（可在任何时间运行）：
资料输入（Excel / 图片 / PDF）
        ↓
自动资料整理 + 导入校验与重复识别（字段来源 / 分层完整度 / 重复拦截）
        ↓
可基础搜索的候选商品池 + 数据质量状态

业务主线：
创建上新任务（平台 / 目标国家 / 中文一级大类 / 单件利润≥金额 / 补充要求）
        ↓
大语言模型生成选品条件卡 ───→ 费用费率与汇率快照（新任务刷新、任务内冻结）
        ↓                                      ↓
智能检索与上新推荐（读取候选商品池；检索 / 重排 / 规则分层）
        ↓
多模态解析与商品事实卡（属性 + 证据 + 置信度 + 审核状态）
        ↓
售价计算（已确认事实 + 任务快照 → 建议售价）
        ↓
受控 Listing 生成（私有资料检索 + 结构化输出）
        ↓
平台规则引擎 → 审核工作台 → Shopify 草稿 / Amazon 上传文件 / 结果反馈
```

### 2. 智能选品方法

```text
大语言模型理解固定条件与补充要求 → 可审阅选品条件卡
关键词检索 + 语义检索 + RRF → 候选 Top-N
Reranker 模型 API → 语义重排
规则引擎 → 硬条件过滤、重复过滤、资料状态、分层与原因
大语言模型 → Top-N 结果解释、比较和边界案例复核
```

系统不采用固定权重总分。大语言模型根据固定条件与本次补充要求输出任务规则；检索和重排负责找出语义更匹配的候选，规则引擎负责执行确定性过滤、读取既有数据质量状态并输出原因。任务级费用、汇率和税费口径只在商品事实确认后的售价计算中使用，不用于候选排序。导入校验是独立资料库流程：它生成缺失告警和重复处理结果，智能选品只读取这些已写入状态。

### 3. 数据与模型边界

商品事实库记录属性、原始值、标准化值、本地化值、证据来源、置信度、冲突状态和审核状态。原始 Excel、图片、PDF、完整海外店铺目录均保留在私有数据区；调用云端 API 时只发送当前任务必要的条件卡、候选摘要、已确认事实和规则片段。所有模型输出均受 JSON Schema 约束，模型不能直接写入平台后台，也不能修改已确认事实、清除导入告警或将重复记录直接写入候选商品库。

汇率、平台费用、物流模板和税费工具以来源、快照和版本受控保存。大语言模型不得自行编造或修改汇率、税率、费用、利润和售价；自动税则推算只产生候选编码与成本预估依据，不替代报关、税务或法律意见。公开权重、参数、许可证和私有化替代方案统一见附件七。

### 4. MVP 可运行范围

1. 分别演示两条支线：导入 10 个候选 SKU 的商品表、图片／PDF，展示自动预填与红色星号提示；独立创建一条固定条件 + 补充要求的上新任务；
2. 在导入页展示字段来源、基础搜索与售价计算资料缺口、重复拦截和疑似重复合并审核；
3. 展示任务条件 JSON、费用费率与汇率快照、检索、重排、P1／P2／P3 与推荐原因；
4. 选择一个 SKU，生成商品事实卡，展示原始值、本地化单位、证据定位、图片核验和冲突；
5. 在商品事实确认后展示建议售价、费用构成、资料缺口和测算版本；
6. 生成 Amazon US 与 Shopify US 草稿；
7. 拦截无依据的保温、防漏、认证等表达；
8. 创建 Shopify 草稿或导出 Amazon 上传文件，展示结果回流。

### 5. 效果验证

- 系统自动预填可识别资料并保留来源；基础搜索、售价计算和 Listing／发布所需资料分层提示，推荐阶段仅读取已有状态；
- 完全重复记录不进入候选商品库，也不参与检索、排序、生成或发布；
- 10 个候选 SKU 可形成不受价格影响、可解释的 P1／P2／P3 结果；
- 5—10 个 SKU 的关键属性可自动结构化并定位来源；
- 新建任务重新获得参考汇率；同一任务的建议售价使用同一份冻结快照；
- 高风险无证据宣称不得进入发布输出；
- 资料齐备时，首版 Listing 草稿进入分钟级流程。
