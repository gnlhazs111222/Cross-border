# 审核格式失败调查与页面说明修正

2026-09-09，基线 bb6991f。提示词 review-qwen-v8、规则 review-hard-v6 不变；未改变审核判断、引用校验或发布授权条件。

## 调查结论

首次浏览器失败调用 cmttqz2z10009qq608hmpj6v5 仅保留 invalid_ai_json 和使用量，原始输出未保存，因此无法回溯具体字段错误。

读取独立验收任务的第 7 版，并验证输入哈希 cddb8a53c094eaff741480c2ffc5f916b44256a2c7bfb9d8a55a8bda6c6da5e1 与失败记录一致。修改实现前做了三次有限诊断调用，全部 blocked，正确引用 capacity。响应中的 title/key=null 被已有规范化逻辑正确处理，未复现首次失败。三次结果不能证明偶发失败已经消失。

诊断工具 `scripts/diagnose-review-output.ts` 限定独立任务和固定输入哈希，每次显式执行调用一次模型并产生费用；仅此测试工具捕获合成验收文案的响应，线上审核不记录原文。诊断产物位于 `artifacts/b-review/browser-acceptance-2026-09-09/diagnostic-*.json`。

## 已修正

- 审核专用错误分类：JSON 语法失败仍为 invalid_ai_json，JSON 可解析但字段校验失败为 invalid_ai_schema。其他模块保留原错误码行为。
- 错误携带最多 8 项安全诊断，仅含校验代码及允许的结构字段路径；不含模型原值、原始错误消息、额外键名、提示词或密钥。Qwen 审核保存到已有 metadata，并在折叠的审核追踪信息中展示。未变更数据库 schema。
- 解析失败仍返回 failed，继续锁定发布，不丢弃问题、不默认通过、不自动重试。
- 服务端模式的编辑说明改为“保存后依据已确认事实重审”；顶部及侧栏不再声称智能功能全为模拟。离线模式保留原模拟说明。
- 审核配置加载或未知时使用中性说明，避免短暂显示规则模式。

## 验证

106 项服务端测试通过；类型检查和凭据检查通过。新增用例验证 JSON 与字段失败区分、校验路径保留、错误内容脱敏、审核失败不能变成批准。首个回归用例在修改前确实因旧错误分类失败。

实际浏览器复测同一 Shopify 任务：

- 第 9 版将标题改成 750ml 后，旧审核失效，发布不可用。
- 主动调用 cmttrh5jr0004qqzsv1joxaoq 返回 blocked；页面展示标题 750ml、规格矛盾、capacity 依据和改为 500ml 的建议。
- 改回 500ml 保存第 10 版后仍需审核；调用 cmttrikef0009qqzstjp2doax 通过，发布恢复可用。
- 页面已观察到新编辑说明、新顶部及侧栏说明和中性加载说明；未执行成功发布。

本轮共 3 次诊断及 2 次页面审核，均为真实调用，审计见 [followup-audit.json](../artifacts/b-review/browser-acceptance-2026-09-09/followup-audit.json)。保留首次失败记录，不以复测成功覆盖。当前任务最终为正确容量第 10 版、审核通过。

本轮修复了诊断缺口和已确认的页面误导；首次偶发模型格式失败的具体原因仍未确定，不能宣称根治。若再次出现，使用新保存的 validationIssues 定位具体约束后再修复。
