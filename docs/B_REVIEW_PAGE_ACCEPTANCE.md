# B 线页面验收清单

日期：2026-09-09。基线提交：bb6991f。

最新状态：2026-09-09 已在 Codex 内置浏览器执行页面验收，未全项通过。正常文案和修正后文案通过；错误容量审核返回 invalid_ai_json，发布保持锁定。详细结果见 [本轮报告](B_REVIEW_BROWSER_RESULT_2026_09_09.md)。以下清单保留原验收设计；“待验证”栏为执行前记录，以本轮报告为准。

## 准备

- 打开 http://localhost:5175/，正常登录，不进入离线比赛演示。
- 演示账户：demo@prismlaunch.local / Demo123456。
- 找到独立验收任务 B-ACCEPT-1788879026672，选择 Shopify 平台。
- 确认事实卡容量为 500ml / 16.9 fl oz，且已确认、允许用于文案。保留事实卡不变。
- 如果找不到任务，先确认使用的是同一后端数据库；不要重置或覆盖其他任务。
- 本轮预计主动运行三次审核，可能产生真实 Qwen 调用费用。刷新和切换页面不应触发审核。

## 操作与预期

| 步骤 | 操作 | 必须观察到的结果 | 实际结果 |
| --- | --- | --- | --- |
| 1 | 正确的 500ml 文案进入“审核与发布”，点击“运行审核” | 等待中有提示；完成后为语义审核通过，发布可用 | 待验证 |
| 2 | 展开审核追踪信息 | 显示 Qwen 模型、review-qwen-v8、review-hard-v6、文案/事实版本和调用记录 ID；区别于文案生成详情 | 待验证 |
| 3 | 回到文案工作室，打开“编辑商品文案”，仅将标题中的 500ml 改为 750ml，点击“保存新版本” | 文案版本增加，保存成功；事实容量仍为 500ml | 待验证 |
| 4 | 返回“审核与发布”，先不运行审核 | 旧通过结果不能继续授权发布，发布按钮不可用；刷新后仍不可用 | 待验证 |
| 5 | 点击“运行审核” | 拦截容量矛盾；问题定位到标题中的 750ml，依据指向 capacity 的 500ml，建议改正；发布不可用 | 待验证 |
| 6 | 刷新页面 | 问题及拦截状态保留，不出现新的审核调用记录 | 待验证 |
| 7 | 编辑标题，将 750ml 改回 500ml 并保存 | 新版本尚未审核，不能直接发布 | 待验证 |
| 8 | 再次运行审核，然后刷新 | 审核通过，发布可用，当前版本和调用记录一致 | 待验证 |

此流程停在修正后审核通过，不需要点击成功发布。若出现 failed 或 needs_human_review，记录原始结果和调用记录 ID，不能将其算作成功识别规格矛盾，也不要反复重试覆盖首次失败。

建议保留通过、修改后失效、容量拦截、修正后通过四张截图，截图中包含版本和审核追踪信息。

## 静态检查发现的待处理项

`src/pages/ListingStudio.tsx` 的编辑弹窗无条件显示“模拟审核检查已知、有证据支持的措辞及预置风险。其他改写均需另行确认，会在本演示中被拦截。”

这段说明沿用旧 Mock 行为，与真实 Qwen 模式允许合理改写的设计不符，可能误导用户。已从代码确认该说明存在，但尚未通过页面观察。应按当前审核模式显示相应说明，并验证离线规则模式与真实审核模式。

## 已有证据与本轮边界

此前真实 HTTP 验收已完成同类流程，记录见 `artifacts/b-review/real-input-fix/acceptance-after.json` 和 `audit-after.json`。这只能证明接口行为，不能代替本清单中的页面、按钮和视觉状态验收。

本轮仅准备服务、核实页面操作入口并建立清单，未新增真实模型调用，未修改商品文案或执行发布。
