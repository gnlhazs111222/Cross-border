# 开源模型与私有化部署说明

> **用途：** 本附件集中说明模型的公开权重、部署位置、许可证核验入口和私有化替代方案。主项目书与其他附件只描述模型职责，不重复放置下载链接或参数信息。  
> **重要边界：** MVP 的候选重排主路径使用百炼 `qwen3-rerank` API；它是云端服务，不应被写成可下载的本地开源权重。严格私有化部署时，才切换到本附件列出的本地重排模型。

## 1. 选型原则

1. **先满足业务边界：** 商品原始 Excel、图片、PDF 和完整店铺目录留在私有存储；云端服务只接收完成字段裁剪后的任务条件和候选摘要。  
2. **模型与规则分工：** 模型用于理解、检索、重排、解析和生成；价格、资料完整度、事实冲突、平台字段和发布准入由规则引擎决定。  
3. **权重与许可证可核验：** 仅列出可公开访问模型卡、可下载权重或官方 API 文档的候选；实际商用前仍须逐项阅读模型卡中的 `LICENSE` 与服务条款。  
4. **MVP 不训练大模型：** 首期采用检索、提示词、JSON Schema、规则与人工反馈实现；积累标注的任务—商品匹配与审核数据后，再评估 LoRA 微调或本地重排微调。

## 2. MVP 采用的云端服务

| 模块 | MVP 选型 | 职责 | 数据边界 | 权重／参数说明 |
|---|---|---|---|---|
| 任务理解、内容生成 | 百炼千问文本／多模态 API | 读懂上新要求、生成条件卡、归并事实、生成结构化 Listing | 仅传递当前任务必要的字段、证据摘要与规则片段 | 云端 API；不作为本地开源权重使用 |
| 候选重排 | 百炼 `qwen3-rerank` API | 按“动态任务条件卡—候选商品摘要”进行 Top-N 语义重排 | 不传完整原始资料与完整店铺目录 | 云端 API；不在项目书中填写未经官方公开的参数规模 |

**官方核验入口：**

- 阿里云百炼模型与服务文档：<https://help.aliyun.com/zh/model-studio/>  
- 阿里云百炼 API 参考：<https://help.aliyun.com/zh/model-studio/developer-reference/>  

> 使用前应在实际账户控制台核对 `qwen3-rerank` 的当前可用地域、调用方式、计费、限流与数据处理条款。产品中保留 `RerankProvider` 接口，因此可在 API 与本地模型间切换。

## 3. 可下载的本地开源权重候选

### 3.1 语义检索：BGE-M3

| 项目 | 内容 |
|---|---|
| 模型 | `BAAI/bge-m3` |
| 角色 | 为候选 SKU、商品事实、平台规则和术语资料建立多语言向量索引；用于语义召回和私有资料检索 |
| 公开权重 | 是，可通过 Hugging Face 下载 |
| 参数规模 | 约 5.68 亿参数（模型卡标注） |
| 许可证 | MIT；以模型卡 `LICENSE` 为准 |
| 模型卡／下载 | <https://huggingface.co/BAAI/bge-m3> |

### 3.2 私有化重排替代：BGE Reranker v2-m3

| 项目 | 内容 |
|---|---|
| 模型 | `BAAI/bge-reranker-v2-m3` |
| 角色 | 严格私有化时，替代云端 `qwen3-rerank` API，对“条件卡—候选商品摘要”进行本地语义重排 |
| 公开权重 | 是，可通过 Hugging Face 下载 |
| 参数规模 | 约 5.68 亿参数（模型卡标注） |
| 许可证 | MIT；以模型卡 `LICENSE` 为准 |
| 模型卡／下载 | <https://huggingface.co/BAAI/bge-reranker-v2-m3> |

> 该模型是私有化替代，并非 MVP 主路径。MVP 优先使用百炼 `qwen3-rerank` API，以降低比赛期间的部署、显存和排序调优成本；若切换本地模型，应使用相同测试集对 P1/P2/P3 一致性、延迟和人工接受率做对比验证。

### 3.3 多模态资料解析：Qwen2.5-VL-7B-Instruct

| 项目 | 内容 |
|---|---|
| 模型 | `Qwen/Qwen2.5-VL-7B-Instruct` |
| 角色 | 解析商品图、包装图和 PDF 页面；抽取属性候选、图片区域／页码证据与置信度；辅助图货一致性复核 |
| 公开权重 | 是，可通过 Hugging Face 下载 |
| 参数规模 | 约 70 亿参数（模型名称中的 7B） |
| 许可证 | 以模型卡中最新 `LICENSE` 为准，并在商用部署前完成法务核验 |
| 模型卡／下载 | <https://huggingface.co/Qwen/Qwen2.5-VL-7B-Instruct> |

### 3.4 私有化文本生成备选：Qwen2.5-7B-Instruct

| 项目 | 内容 |
|---|---|
| 模型 | `Qwen/Qwen2.5-7B-Instruct` |
| 角色 | 严格私有化时，用于任务条件卡生成、事实归并、英文 Listing 草稿及解释输出 |
| 公开权重 | 是，可通过 Hugging Face 下载 |
| 参数规模 | 约 70 亿参数（模型名称中的 7B） |
| 许可证 | 请以模型卡中最新 `LICENSE` 为准，并在商用部署前完成法务核验 |
| 模型卡／下载 | <https://huggingface.co/Qwen/Qwen2.5-7B-Instruct> |

## 4. 下载与运行方式示例

公开权重模型可使用 Hugging Face 的 `huggingface_hub` 下载。以下为**技术验证示例**；实际部署应固定版本、校验哈希、在隔离网络中下载，并遵守对应许可证。

```python
from huggingface_hub import snapshot_download

# 语义检索模型
snapshot_download(repo_id="BAAI/bge-m3", local_dir="./models/bge-m3")

# 本地重排替代模型
snapshot_download(
    repo_id="BAAI/bge-reranker-v2-m3",
    local_dir="./models/bge-reranker-v2-m3"
)

# 多模态资料解析模型
snapshot_download(
    repo_id="Qwen/Qwen2.5-VL-7B-Instruct",
    local_dir="./models/Qwen2.5-VL-7B-Instruct"
)
```

## 5. 推荐的部署分层

```text
私有数据区
  ├─ 原始 Excel / 图片 / PDF / 店铺目录
  ├─ 对象存储 + 商品事实库 + 向量库
  ├─ BGE-M3：本地向量检索
  ├─ Qwen2.5-VL：本地资料解析（可选）
  └─ BGE Reranker：严格私有化时的本地重排（可选）

受控 API 区
  ├─ 百炼千问：任务理解、事实归并、受控生成
  └─ 百炼 qwen3-rerank：Top-N 语义重排

规则与审核区
  └─ 价格／资料／冲突／字段／风险／发布状态的确定性判断
```

调用云端重排或生成 API 时，系统只发送：任务条件卡、字段裁剪后的候选摘要、已确认事实及必要的规则片段；不发送原始文件二进制、完整海外店铺目录、全部候选库或未脱敏的商业资料。

## 5.5 汇率、税费与售价计算的工具边界

- 大语言模型和重排模型不负责生成汇率、税率、平台费用、利润或售价数值；
- 生成选品条件后，费用快照服务通过受控工具获取最新可用参考汇率，并将其与平台费用版本、履约模板版本、税费计算口径一起冻结为本任务快照；新任务再获取新的参考汇率；
- 售价计算服务在商品事实确认后调用受控税费工具，使用商品事实卡中的供货价、用途、材质、原产国、申报价值、重量、包装尺寸和自动推算税则候选完成进口关税预估，并结合任务最低单件利润反算建议售价；
- 这部分属于确定性工具编排，不要求为税则号或售价计算训练新的排序模型；资料不足时返回“无法计算建议售价”和待补字段，不编造结论；建议售价不参与商品排序。

## 6. 评测与后续微调

MVP 建议建立一个小型人工标注集：每条记录包含“上新任务、候选商品、人工选择的 P1/P2/P3、拦截原因、资料补齐结果”。评测指标包括：

- Top-N 推荐的人工接受率；
- 硬条件误放行率；
- 资料缺失告警准确率；
- 事实冲突发现率；
- 无证据宣称拦截率；
- 单 SKU 首版草稿耗时。

当积累足够的真实审核反馈后，可针对任务条件解析或本地重排模型进行 LoRA／轻量微调；在此之前，项目不承诺训练一个需要大量标注数据的新排序模型。

## 7. 许可证与复现提醒

- “可下载权重”不等同于可忽略许可证；比赛演示和后续商用均应以模型卡中的最新许可证为准。  
- Qwen 系列、BGE 系列和云端 API 的许可／服务条款彼此独立，应分别核验。  
- 模型卡、下载页面和 API 文档可能更新；提交代码时建议在 `THIRD_PARTY_NOTICES.md` 中锁定实际使用的模型版本、提交日期和许可证文本快照。

