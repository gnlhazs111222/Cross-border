# 棱镜上新｜grill-me 逻辑冲突审问与修订记录

> 审问原则：不接受“以后再补”“模型会处理”“先做出来再说”作为流程答案。每一项都要求明确：谁负责、何时发生、缺什么能否继续、输出能否被下游使用、谁拥有最终决定权。

## 1. 待补资料到底能不能保存？

**尖锐问题**  
如果字段不齐就不能保存，运营人员如何留存尚未整理完的供应商资料？如果能保存，又凭什么禁止它被误当作可选商品？

**不解决的后果**  
会同时出现“导入不可用”和“缺失资料进入候选池”两种相反行为，导致资料丢失或带病推荐。

**最终决策**  
导入批次与待补记录始终可保存、可追溯；资料状态与可用性分开管理：`draft_blocked` 表示身份字段无法稳定识别，`needs_completion` 表示候选池准入字段不齐，`search_ready` 才表示资料可进入后续准入判断。

**已同步位置**  
项目书、业务流程图的“导入与归档／资料校验／候选商品池”、技术路线图的“资料库支线”。

## 2. 候选池准入为什么要和售价资料绑在一起？

**尖锐问题**  
成本、重量、尺寸、申报价值尚缺时，为什么商品连是否匹配“平台、国家、一级类目和商品偏好”都不能被判断？

**不解决的后果**  
选品能力被可选的利润测算卡死，且业务人员会为了通过校验虚填成本或尺寸。

**最终决策**  
候选池准入字段与售价计算字段解耦。只有身份信息、任务可匹配的商品基础字段和去重结论决定能否入候选池；成本、申报价值、重量、尺寸仅在主动启用售价测算时校验。

**已同步位置**  
项目书的资料状态机、字段分层和售价说明；两张流程图的资料库与价格模块。

## 3. 为什么还没选 SKU 就要冻结汇率、费用和税费？

**尖锐问题**  
任务刚创建时，SKU 的成本、重量、尺寸、税则和申报价值仍未知；若此时称“冻结费用”，会不会伪造一份貌似完整、实则无法用于实际计算的 SKU 审计记录？

**不解决的后果**  
把任务级市场／配置口径与 SKU 级商品事实混在一张快照中：要么形成伪审计记录，要么为每个 SKU 静默改变汇率和费用口径，导致同一任务的价格不可比。

**最终决策**  
在创建任务时，按目标平台、目标国家及履约／服务档案冻结**任务级定价上下文快照**：参考汇率及其来源／生效日期／抓取时间、平台费用配置版本、物流／履约模板版本和税费计算口径版本。该快照不含 SKU 成本、重量、尺寸、税则或申报价值。选中 SKU 并主动启用售价计算时，系统只将其已确认的 v2 计算事实与该任务快照组合；若实质变更平台、国家、履约档案，或运营人员明确要求重新定价，必须显式生成新的任务快照版本，不能静默覆盖。

**已同步位置**  
项目书的任务表单、费用快照、售价与页面说明；业务流程图的任务创建与快照节点；技术路线图的任务与售价阶段。

## 4. 最低单件利润凭什么阻塞任务创建和选品？

**尖锐问题**  
最低利润是价格策略，还是商品是否匹配的事实？若运营暂不测算价格，为什么不能生成 Listing 草稿？

**不解决的后果**  
一个可选经营偏好会错误变成全流程硬依赖，并污染召回、重排和推荐逻辑。

**最终决策**  
最低单件利润改为可选的售价计算参数；它不参与任务准入、候选池准入、召回、重排或推荐结论。售价计算被定位为可选增强模块，不能阻塞 Listing 审核。

**已同步位置**  
项目书的任务表单、选品、售价页；业务流程图和技术路线图的任务／价格边界。

## 5. 模型、事实卡还是审核工作台，谁能说“可以发布”？

**尖锐问题**  
若模型直接输出 `listing_allowed: true`，那么规则校验和人工审核到底还有没有否决权？

**不解决的后果**  
模型会越权把不完整、风险未关闭或事实未确认的内容推进发布链路。

**最终决策**  
多模态和文案模型只能产生 `review_status: pending` 的候选；规则引擎负责风险与平台校验，审核工作台和人工审核负责最终进入发布队列。任何模型、推荐结论或事实卡都不能直接给出可发布结论。

**已同步位置**  
项目书的多模态 JSON 合同、Listing／审核／发布章节；业务流程图的多模态、审核和发布模块；技术路线图总体边界。

## 6. v2 是覆盖 v1，还是每个任务独立的事实快照？

**尖锐问题**  
不同目标平台、图片证据、人工确认可能不同；若 v2 覆盖 v1，后续任务如何复现当时的事实来源？

**不解决的后果**  
商品基础资料会被任务特定判断污染，审计时无法还原“哪个任务在何时用过什么证据”。

**最终决策**  
v1 是候选池中可检索的基础结构化底稿；仅 `search_ready + unique` 时创建。v2 按“任务 + SKU”从 v1 派生，保存多模态证据和人工确认，不覆盖、不改写 v1。

**已同步位置**  
项目书的事实卡版本策略；业务流程图的多模态模块；技术路线图的事实卡版本合并模块。

## 7. 导入已经“解析图片”，为什么还要多模态模块？

**尖锐问题**  
如果导入阶段既提取图片文字又建立事实，后续多模态模块的唯一职责还成立吗？两处 OCR 结论冲突时听谁的？

**不解决的后果**  
同一能力存在双责任主体，证据来源不可追溯，且会出现无法解释的字段覆盖。

**最终决策**  
导入阶段只解析表格／可读文档字段并建立图片资源关联；图片识别、OCR、图片—资料核验只由多模态解析模块承担。事实卡服务只负责保存、版本化合并和输出。

**已同步位置**  
项目书的导入、事实卡和技术方案；两张流程图的资料导入与多模态阶段。

## 8. 原始敏感资料是否会被整包发送到云端？

**尖锐问题**  
“云端模型仅接收当前任务所需资料”具体到图片、PDF、供应商资料时，最小必要范围、日志与例外处理在哪里？

**不解决的后果**  
数据边界停留在口号，实际操作会把完整目录、整份规格书或敏感文件无差别外发。

**最终决策**  
原始 Excel、图片、PDF 与完整目录留在私有数据区。云端文本模型只接收任务条件、候选摘要、已确认事实和规则片段；云端多模态模型只接收当前选中 SKU 的必要图片、裁剪区域或文档页面，并记录传输范围与调用日志。敏感资料使用本地模型或转人工。

**已同步位置**  
项目书的数据边界；技术路线图的多模态调用和总体技术原则。

## 9. 成本、申报价值和税费为什么会进入消费者文案？

**尖锐问题**  
这些数据是内部测算或申报口径，不是消费者卖点；把它们送给写作模型到底要生成什么？

**不解决的后果**  
会造成敏感信息暴露，也会让模型误把成本、税费或申报信息写入标题、卖点和详情。

**最终决策**  
成本、申报价值、税费仅供售价计算、申报等受控模块使用；不进入标题、卖点、详情文案的生成上下文。消费者文案只读取经审核允许的 `SKU + v2 版本 + 平台` 结构化事实。

**已同步位置**  
项目书的 Listing 数据合同和安全边界；业务流程图和技术路线图的价格／Listing 模块。

## 10. RAG 为什么要检索当前 SKU 的事实？

**尖锐问题**  
已经有版本化事实卡，为什么还要通过语义检索“找回”当前商品事实？相似商品的片段会不会被混入？

**不解决的后果**  
会把结构化、可追溯的事实读取降级为概率性召回，诱发串 SKU、串版本和幻觉。

**最终决策**  
Listing 使用 `SKU + v2 版本 + 平台` 直接读取当前商品的已确认结构化事实；RAG 仅服务平台规则、本地化术语和写作模板，并保留来源引用。

**已同步位置**  
项目书的 Listing 工作台和技术路线图的规则与术语检索增强模块。

## 11. 风险引擎能不能替企业作法律结论？

**尖锐问题**  
“检查品牌／知识产权／商家授权”是否暗示系统能确认侵权与否、授权是否有效？

**不解决的后果**  
技术筛查被误读为法律意见，形成越权承诺和错误的自动放行。

**最终决策**  
风险引擎只筛查第三方品牌词、可疑 Logo、授权材料缺失、敏感内容与平台字段问题，并提示人工复核；系统不自动给出法律结论。

**已同步位置**  
项目书风险审核章节；技术路线图的“平台规则与风险引擎”。

## 12. “免密授权”是否会被理解为绕过认证？

**尖锐问题**  
如果系统宣称“免密”，用户是否会误以为可以绕过验证码、二次验证或平台权限？

**不解决的后果**  
方案既不准确，也会给安全设计留下不应存在的承诺。

**最终决策**  
统一改为“平台授权与连接器”：通过平台官方 OAuth 2.0 与最小权限访问令牌执行连接；不收集、不明文保存账户密码，不绕过平台认证、验证码、二次验证或权限校验。

**已同步位置**  
项目书发布章节；技术路线图审核、发布与反馈阶段。

---

## 修订后的不可违背规则

1. 可保存不等于可建事实卡，不等于可进入候选池，更不等于可发布。
2. 只有 `search_ready + unique` 的 SKU 才能创建 v1 并进入候选池。
3. v2 是任务级增强快照，永不覆盖 v1。
4. 规则决定推荐结论，重排只调顺序，模型只生成候选与解释。
5. 任务创建时冻结定价上下文，售价计算仍按需运行；价格目标和快照不污染选品与文案。
6. 当前 SKU 的事实必须直接按版本读取；RAG 不能替代事实卡。
7. 模型不得自行宣布发布可用；审核工作台是唯一的发布放行入口。
8. 云端调用遵循最小必要数据原则，并保留范围与日志。
