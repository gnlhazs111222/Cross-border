# 棱镜上新 PrismLaunch｜三份文档修改建议

> 检查对象：`01_业务流程图.html`、`02_技术路线图.html`、`棱镜上新_PrismLaunch_项目书.md`  
> 检查日期：2026-08-27  
> 说明：以下行号以当前版本为准。手工修改后行号可能变化，可用文中关键词继续搜索定位。

## 一、项目书需要修改的位置

### 1. 导入阶段是否识别图片，前后矛盾

**位置：**

- 第 127 行：“自动识别 SKU、商品字段……”
- 第 269 行：“从包装图识别容量”
- 第 283 行：`package.jpg:OCR`
- 第 925 行：明确说 v1 不识别图片
- 第 1027 行：图片识别放在选品后的多模态阶段

**问题：** 前面说导入阶段做图片 OCR，后面又说图片只关联、不识别。

**建议统一为：**

> 资料导入阶段解析 Excel、CSV 和可直接读取的 PDF 文本，商品图片仅完成上传、文件指纹计算和 SKU 关联，不执行图片 OCR 或视觉判断。运营人员选中 SKU 后，再由多模态解析模块读取图片和扫描型 PDF。

**具体修改：**

- 第 269 行删除“从包装图识别容量”，容量改为来自 Excel/PDF；
- 第 283 行删除 `package.jpg:OCR`，或者把该例子移到 v2 多模态事实卡部分；
- 第 127 行把“自动识别商品字段”改成“从表格和可读文档中提取商品字段，并关联图片资源”。

### 2. 缺资料能不能保存，规则冲突

**位置：**

- 第 88 行：没补齐不能保存
- 第 98 行：缺少关键字段不可保存
- 第 129 行：资料未补齐不能保存
- 第 270 行：又出现 `needs_completion`
- 第 866 行：必填项未完成不能保存
- 第 887—895 行：多次强调不允许保存

**问题：** 如果不能保存，就无法长期保留待补资料，也无法体现“资料质量管理”。

**建议统一为三级状态：**

```text
blocked
SKU、商品名称、类目等身份字段缺失
→ 保存导入记录，但不进入候选池

needs_completion
价格、重量、包装尺寸等资料缺失
→ 可以保存并进入候选池，但只能进入 P2，不能生成最终发布包

ready
身份和关键资料齐全
→ 可以正常参加选品、事实核验和 Listing 流程
```

第 98 行建议改为：

> 接收 Excel、图片和 PDF，按 SKU 归集并校验字段。所有导入批次均可保存；身份字段缺失的记录标记为 `blocked`，不进入候选池；其他关键资料缺失的记录标记为 `needs_completion`，保留待补状态。

第 866 行建议改为：

> 系统允许保存未完成的导入记录，但只有达到候选池准入条件的商品才能创建基础事实卡并参加智能选品。

### 3. 最低利润作为选品必填条件，业务逻辑不成立

**位置：**

- 第 50 行：选品需求包含“单件利润至少 5 美元”
- 第 108 行：任务级售价计算
- 第 311—325 行：最低利润作为固定任务条件
- 第 359 行：创建任务时建立费用快照
- 第 593—655 行：售价反算
- 第 945—977 行：最低利润必填，但明确不参与推荐
- 第 1181 行：演示脚本继续把它作为选品输入

**问题：** 只要把售价调高，任何商品都能满足“利润至少 5 美元”，因此它不能单独作为选品条件。

**推荐改法：**

- 从“创建上新任务”的必填条件中删除最低单件利润；
- 将其放到选中 SKU 后的“售价测算”页面；
- 售价测算作为可选增强功能，不影响选品主线。

创建任务建议改为：

```text
目标平台 *
目标国家 *
目标平台商品类目 *
目标零售价区间（选填）
其他补充要求（选填）
```

如果保留最低利润，必须同时增加“目标售价上限或目标零售价区间”，并让利润可行性参与 P1/P2/P3 分层。

### 4. P1/P2/P3 与 Top 3 直接排序不一致

**位置：**

- 第 100 行：优先推荐、观察、暂缓
- 第 427—429 行：只有排名 1、2、3
- 第 845 行：“直接输出排序推荐结果”
- 第 997 行：只输出推荐顺位
- 第 1182 行：演示 Top 3

**建议统一保留 P1/P2/P3：**

```text
P1 优先上新：任务匹配，资料可用
P2 补充后处理：任务匹配，但存在待补资料
P3 暂缓评估：疑似重复、关键冲突或硬条件不符
```

第 427 行的表格增加“推荐层级”列；第 845 行改成：

> 智能选品（输出 P1/P2/P3 分层、组内排序和推荐原因）

### 5. 让模型直接输出 `listing_allowed=true` 不安全

**位置：** 第 532—535 行。

**问题：** 这相当于让模型自行批准某项事实进入 Listing。

**建议把模型输出改成：**

```json
{
  "name": "visible_capacity_label",
  "value": "500 ml",
  "result_id": "MM-001",
  "extraction_status": "extracted",
  "review_status": "pending",
  "candidate_for_listing": true
}
```

并补充：

> `listing_allowed` 只能由事实卡服务在完成跨来源一致性检查或人工确认后设置，不能由多模态模型直接输出。

### 6. 云端模型和私有数据边界矛盾

**位置：**

- 第 205 行：云端 API 只接收条件卡、摘要、已确认事实和规则
- 第 502 行：多模态模块读取图片和 PDF
- 第 1027 行：识别商品图、包装图和 PDF

**建议第 205 行改成：**

> 原始资料保存在私有数据区。调用云端文本模型时，仅发送当前任务必要的条件卡、候选摘要、已确认事实和规则片段；调用云端多模态模型时，仅发送当前选中 SKU 所需的图片、裁剪区域或文档页面，并记录发送范围、调用目的和处理结果。涉及敏感资料时使用本地模型或人工处理。

### 7. 10 个 SKU 的验收指标自相矛盾

**位置：**

- 第 209—210 行：导入中包含缺失和重复
- 第 221 行：缺失和重复不进入候选池
- 第 222 行：又说 10 个 SKU 都进入候选池
- 第 223 行：10 个候选 SKU 都参加排序

**建议改为：**

> 系统可处理 10 条导入记录，并分别输出 `ready`、`needs_completion`、`blocked`、`duplicate` 和 `possible_duplicate` 状态；完全重复、身份字段缺失和待合并商品不进入检索索引，其余商品按资料状态参加推荐分层。

不要写“10 个都进入可用候选池”。

### 8. 供货成本和申报价值不应进入营销文案

**位置：**

- 第 469 行：供货成本可作为价格优势表达
- 第 471 行：申报价值可作价值说明
- 第 563—571 行：允许进入 Listing
- 第 1089—1090 行：再次允许相关市场表达
- 第 1141 行：把成本、申报价值列入 Listing 使用边界

**建议统一为：**

| 字段 | 用途 | 边界 |
|---|---|---|
| 供货成本 | 仅用于内部售价和利润测算 | 不发送给 Listing 生成模型，不进入消费者文案 |
| 申报价值 | 仅用于内部税费预估和申报资料 | 不用于营销文案，不用于“商品价值”表达 |

第 563 行删除“成本、申报价值均可作为 Listing 信息或卖点依据”。

### 9. v2 事实卡不应完全绑定单次任务

**位置：**

- 第 458 行：v2 为当前任务服务
- 第 580 行：v2 为当前上新任务提供
- 第 1020—1021 行：v2 更新与任务编号绑定

**建议改为：**

> v2 是 SKU 级、可复用的增强事实卡版本。上新任务不复制事实卡，而是记录本任务引用的 `fact_card_version_id`。平台字段、售价和 Listing 草稿继续按任务保存。

### 10. 平台合规和知识产权能力描述太强

**位置：** 第 68 行、第 157—159 行。

**建议改成：**

> 系统对第三方品牌名称、可疑 Logo、敏感词和授权材料缺失进行风险筛查并提示人工复核，不替代法律判断。高风险项未完成人工确认前，系统暂缓生成发布包。

把“是否符合国外平台要求”改成：

> 是否符合系统当前保存的、带版本号的平台字段与内容规则。

### 11. 类目粒度太粗

**位置：** 第 311 行、第 323 行、第 944 行。

**建议增加：**

```text
中文一级商品大类：家居与厨房
目标平台商品类目：Water Bottles
平台类目节点／Product Type：预配置的比赛演示节点
```

一级类目用于初筛，平台叶子类目用于字段校验和 Amazon 文件导出。

### 12. 两处文字错误

- 第 108 行：“建议售价。；”改成“建议售价。”
- 第 611 行：“税率冻结”改成“汇率冻结”。

### 13. 个人信息公开风险

**位置：** 第 14 行。

正式报名材料可以保留；公开 GitHub、公开演示版和分享版应脱敏。

## 二、业务流程图需要修改的位置

### 14. A1 资料输入错误地包含图片 OCR

**位置：** `01_业务流程图.html` 第 28 行。

把“自动提取表格、图片与文档中的可识别信息”“从表格、图片文字和文档自动预填字段”改成：

> 解析表格和可直接读取的文档字段，建立图片与 SKU 的资源关联，并保留原始文件。图片 OCR 和视觉判断由后续多模态解析模块负责。

### 15. A2“不能保存”与条件卡“保留待补”冲突

**位置：** 第 36 行、第 73 行。

第 36 行建议改成：

> 所有导入结果均保存到导入批次；身份字段缺失时标记为 `blocked`，不进入候选池；其他资料缺失时标记为 `needs_completion`，并在后续推荐和发布环节继承该状态。

### 16. 创建任务中删除最低利润必填项

**位置：** 第 55 行、第 77 行、第 111 行。

建议：

- 第 55 行删除“最低单件利润”；
- 费用与汇率快照不要在任务创建后立即执行；
- 将“费用快照 + 售价测算”放到选择 SKU、完成事实确认之后；
- 售价测算标记为可选增强模块。

### 17. 智能推荐模块的安全边界太弱

**位置：** 第 86 行。

当前只有“给出排序理由”，建议改成：

> 推荐模块只读取候选池摘要和资料状态；不得修改商品事实，不得清除导入告警，不得让完全重复或待合并商品进入结果。重排模型仅调整相关性顺序，P1/P2/P3 由规则引擎根据硬条件和资料状态确定。

### 18. 模型不能直接输出 `listing_allowed`

**位置：** 第 104—105 行。

把：

```json
"listing_allowed": true
```

改成：

```json
"review_status": "pending",
"candidate_for_listing": true
```

### 19. 弹窗可能被现场浏览器拦截

**位置：** 第 141 行、第 147 行，使用了 `window.open`。

建议改成当前页面内的 Modal、Drawer 或固定详情面板。如果暂时不改代码，页面提示改为：

> 点击查看模块详情；若浏览器拦截弹窗，请查看项目书中的静态流程说明。

## 三、技术路线图需要修改的位置

### 20. 检索技术过重

**位置：** `02_技术路线图.html` 第 46 行、第 112 行。

当前为：

```text
Elasticsearch + BGE-M3 + Milvus + RRF
```

建议比赛版替换为：

> PostgreSQL 结构化过滤 + 关键词匹配 + qwen3-rerank。对于 10 个 SKU，可将硬条件过滤后的候选摘要直接送入重排模型；pgvector、RRF 和独立搜索集群作为后期扩展。

底部基础设施改成：

> PostgreSQL + 本地文件／MinIO：保存事实卡版本、文件证据、候选摘要和审计日志。

### 21. Celery、Redis 可以标为后期增强

**位置：** 第 61 行、第 111 行。

如果团队没有现成经验，建议改为：

> MVP 使用数据库任务状态 + FastAPI 后台任务处理单 SKU 解析；Celery + Redis 在并发 SKU 和可靠重试需求增加后引入。

### 22. 云端多模态与私有数据原则冲突

**位置：** 第 62 行、第 118 行。

第 118 行建议改成：

> 原始文件保存在私有数据区。云端文本模型只接收条件卡、候选摘要、已确认事实和规则片段；云端多模态模型仅接收当前选中 SKU 所需的图片、裁剪区域或文档页面，并记录数据范围、用途和调用日志。敏感资料使用本地模型或人工处理。

### 23. Listing 生成不应通过 RAG 检索当前 SKU 事实

**位置：** 第 80 行。

建议改成：

> 后端按 `SKU + fact_card_version + listing_allowed=true` 直接读取结构化事实；RAG 仅用于检索平台规则、本地化术语和写作模板，并保留规则来源。

### 24. “MCP 工具网关”不是数据来源

**位置：** 第 78 行。

建议改成：

> 版本化费用配置 + 明确的汇率数据源。MCP 仅作为可选调用接口，不作为汇率或税费来源；所有数值记录来源、抓取时间、生效日期和版本。

比赛阶段如果还没确定正式 API，可写：

> MVP 使用带来源说明的固定演示快照，后续接入官方或授权数据 API。

### 25. “免密授权”表述不准确

**位置：** 第 97 行、第 100 行。

把：

> 免密授权与平台连接器  
> OAuth 2.0 / SSO / 官方授权令牌

改成：

> 平台授权与连接器  
> Shopify OAuth 2.0 + 最小权限访问令牌

SSO 与 Shopify 店铺 API 授权关系不大，建议删除。

### 26. 风险引擎不能声称自动确认知识产权和授权

**位置：** 第 96 行。

把：

> 检查品牌/知识产权与商家授权

改成：

> 筛查第三方品牌词、可疑 Logo、授权材料缺失和敏感内容，并提示人工复核；系统不自动给出法律结论。

### 27. 技术路线图过长，嵌入项目书后文字偏小

**位置：** `02_技术路线图.html` 整体；CSS 主要在第 8 行，技术卡正文为 12px；导出 PNG 为 1166×2100。

建议拆成两张：

1. MVP 核心技术路线：导入、选品、事实卡、Listing、审核；
2. 扩展与基础设施：异步任务、对象存储、向量检索、平台连接器。

## 四、推荐修改顺序

- [ ] 决定最低利润和售价计算是否保留
- [ ] 统一“导入阶段不识别图片”
- [ ] 统一 `blocked / needs_completion / ready`
- [ ] 统一 P1/P2/P3
- [ ] 修正云端模型的数据边界
- [ ] 删除模型直接输出的 `listing_allowed=true`
- [ ] 将成本和申报价值移出 Listing 生成上下文
- [ ] 精简比赛技术栈
- [ ] 同步修改业务流程图和技术路线图
- [ ] 重新导出两张 PNG 并检查项目书排版

