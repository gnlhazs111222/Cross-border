# PrismLaunch 当前项目进度｜供网页版 GPT 阅读

更新时间：2026-09-08 10:12（北京时间）

本文独立概括项目现状，覆盖真实文件导入、人工事实确认、完整上新 Demo 和最新比赛展示优化。请据此评价进度，区分已经执行的本地逻辑、仍然模拟的智能能力，以及尚未实现的生产能力。

## 1. 当前结论

**PrismLaunch 已完成可稳定演示的比赛版本：真实固定模板 XLSX / CSV 导入 → 商品资料与选品 → 事实卡与人工确认 → 定价 → 文案 → 风险拦截 → 修订重审 → 模拟发布和 CSV 下载。**

已有中英文、浏览器状态保存、内置数据回退。主线和缺事实支线均实际运行验证。最近一轮主要改善演示体验和解释清晰度，没有继续新增业务模块。

- 最新代码提交：`4a2920f`，`chore: polish PrismLaunch demo presentation`。
- 分支：`pyc`。
- GitHub：`https://github.com/gnlhazs111222/Projects/tree/pyc`。
- 本次核对时，本地与远端代码提交一致，最新版已推送。
- 最近完整验证：TypeScript、生产 build 全部通过，**28 项测试全部通过，26.9 秒**。
- 演示材料：3 分钟讲稿、5 分钟讲稿、20 个评委问答、11 张 1920×1080 的实际 UI 截图。

现在可以用于比赛、课堂或产品流程展示。它有真实文件处理和状态约束，并非单纯静态页面；但没有真实 LLM、OCR、平台发布或生产后端，不能称为已上线的 AI 电商系统。

本次仅更新进度说明，没有新增代码、重复跑测试或执行 Git 推送；上述验证结果来自最近开发回合，本次重新核对了记录、Git 远端及本地预览状态。

## 2. 已完成的阶段

| 阶段 | 已完成内容 | 对应状态 |
| --- | --- | --- |
| 初版 Demo | 五个工作区、预置数据、完整模拟上新、双语、localStorage、CSV | 主流程已跑通 |
| 真实导入 | 固定模板 XLSX / CSV 解析、校验、逐行报告、SKU 去重、目录替换 / 追加 | 提交 `8c952fe`，当时 16 项测试通过 |
| 人工 Fact Review | 编辑、补齐、确认、拒绝、人工来源记录、下游自动重新判断及失效 | 提交 `b47b061`，当时 22 项测试通过 |
| 比赛展示优化 | 主操作前置、下一步引导、Real / Mock 说明、风险状态强化、响应式、讲稿和截图 | 最新提交 `4a2920f`，28 项测试通过 |

最新提交在原稳定版本上追加，没有重写原提交或重构业务架构。本轮已达到停止条件，没有继续主动添加新功能。

## 3. 产品想说明的问题

供应商资料可能缺字段、存在重复，也可能包含无证据支持的宣传。PrismLaunch 演示如何回答：

1. **为什么推荐这个商品？** 展示任务条件、匹配理由和扣分因素。
2. **为什么允许使用这项事实？** 展示值、来源、确认状态和文案权限。
3. **为什么此时可以发布？** 当前事实和文案版本必须满足本地规则，修订后需要重新审核。

```text
内置数据 / 真实 XLSX、CSV 文件
  → 固定字段解析、校验、精确 SKU 去重
  → Materials 与候选池
  → 固定 Launch Task / Top 3
  → 选择商品
  → FactCard V1 / V2
  → 人工补齐、确认或拒绝
  → Pricing Blocked / Ready
  → Amazon / Shopify 文案
  → Review 阻断
  → 修订 → 重新审核
  → Mock Publish / Amazon CSV
```

SKU 是具体规格商品的编号；Evidence 是支持字段的来源资料；FactCard 是整理后的商品事实、来源和使用状态。

## 4. 五个工作区已实现什么

### Materials / 商品资料

- 保留原 10 个内置 SKU，支持一键载入和 Reset。
- 展示商品名、SKU、类目、颜色、容量、资料状态和重复状态。
- 支持中英文商品名、类目、颜色及 SKU 搜索，候选 / 排除筛选和商品详情。
- 支持真实固定模板 Excel / CSV 导入，先预览校验结果，再确认更新目录。
- 缺包装重量商品仍可保存，显示 Missing Data / Pricing Blocked，并可通过“核对事实”进入补齐流程。
- 当前目录的候选数量和排除数量按实际数据计算。

### Launch Tasks / 上新任务

- 固定任务 `PL-DEMO-001`，Amazon US / United States / Home & Kitchen。
- 条件为黑色、简约通勤、约 16 oz、无吸管、优先包装信息完整，最低单位利润 USD 5。
- 推荐 Top 3，并显示具体匹配和扣分理由。
- 主角为 `LM-KT-BTL-001-BLK-500`，黑色不锈钢随行杯，500ml / 16.9 fl oz，无吸管。
- 评分采用固定规则：完整匹配 94 分，带吸管扣 12 分，容量偏离目标扣 16 分，颜色不符扣 23 分。
- 缺资料、重复、疑似重复和类目不符的商品不会进入推荐。

当前没有自由需求输入、多任务或真实 AI 推荐。其他商品在人工补齐后也可能得到 94 分，同分时按固定演示规则优先主角；94 不是模型置信度或销量预测。

### Evidence & Facts / 证据与事实

- **V1** 包含 11 个基础字段：颜色、容量、材质、吸管、原产国、包装重量、长宽高、采购成本、申报价值。
- 导入商品的 V1 字段来自实际文件，保留文件名、工作表、行号和字段名。
- **V2** 补充包装内含、表面工艺、杯盖类型和待确认的防漏宣称。
- 分析 V2 不覆盖 V1；人工明确修改基础字段会更新 V1 并同步 V2 继承的基础字段。修改 V2 增强字段不会反写 V1。
- 人工 Fact Review 支持查看来源、编辑、补齐、确认、拒绝。
- 页面区分 Supplier File、Mock Evidence、Manual Confirmation。

PDF / 图片补充字段仍是 Mock，没有实际 OCR 或视觉理解。为保持原主角演示步骤，其既有 V2 确认状态保留；其他商品新生成的补充事实先待确认。导入解析或 Confirmed 都不代表独立验证了供应商真实性。

### Listing Studio / 文案工作室

- 展示本地定价公式结果及费用假设。
- Amazon / Shopify 分别保存文案，支持标题、卖点、描述、属性和事实来源。
- 正常文案仅使用 `status === 'Confirmed' && allowed === true` 的事实，按模板组装。
- 采购成本、申报价值、重量、尺寸等内部信息不会写进消费者文案。
- 支持手动编辑和重新生成，修改后旧审核结果失效。
- 美国市场文案和 CSV 保持英文，界面语言可以切换。

首次 Amazon 草稿特意加入 `100% leakproof` 作为独立风险注入。UI 明确解释该句不是正常通过事实授权生成的内容，也不是本版本真实 LLM 产生的幻觉。

### Review & Publish / 审核与发布

- R001 拦截 `100% leakproof`，说明缺少支持绝对化性能宣称的证据。
- R002 拦截与确认模板不一致的其他改写；尚无通用语义审核能力。
- 风险出现时 Publish 禁用；应用建议修订后仍为 Review required。
- 必须重新审核当前文案版本，通过后才能模拟发布。
- Amazon 生成可真实下载的演示 CSV；Shopify 显示 `SHOP-DEMO-1042 / Draft`。
- 没有向真实平台发送上架请求，CSV 也不是已通过官方验证的上传模板。

## 5. 真实导入的范围和样例

仅支持以下固定 14 列，名称大小写必须一致，顺序可调整，不做语义猜测：

```csv
sku,productName,category,color,capacityMl,material,hasStraw,countryOfOrigin,supplierCost,declaredValue,packagingWeightKg,packageLengthCm,packageWidthCm,packageHeightCm
```

- XLSX：单工作表；CSV：UTF-8、英文逗号，支持标准引号、逗号和单元格内换行。
- 最多 2 MB、500 行，单字段最多 220 字符；不支持公式或错误单元格。
- 缺 SKU、商品名等必填值，或数字 / 布尔值非法，标记 Invalid row 并显示原因。
- 缺重量或尺寸可以保存，但资料和定价受阻。
- 替换模式只保留本文件的有效商品；追加模式保留现有商品，仅添加新 SKU。
- 精确 SKU 重复会跳过，保留首个有效值，不覆盖成本。
- 成功导入重置旧任务及下游结果；取消、坏文件或没有有效新商品时不改变原业务状态。

样例文件：

```text
public/demo/prismlaunch-supplier-demo.xlsx
public/demo/prismlaunch-supplier-demo.csv
```

样例有 8 行：6 Ready、1 Missing Data、1 Duplicate、0 Invalid，默认替换后保存 7 个商品。追加到原 10 个内置 SKU 时新增 3 个，目录变为 13 个；再次追加不会继续增加。

导入不是上传后继续展示原始内置数据：测试还实际导入了一个 350ml 自定义商品，USD 12.00 采购成本和 USD 6.75 申报价值进入 V1，模拟建议价随之变为 USD 23.00。

## 6. 人工确认已经形成真实小闭环

继续使用 `Fact.status` 和 `Fact.allowed`，没有另一套重复权限体系。

| 状态 | 对正常文案的影响 |
| --- | --- |
| Confirmed / 已确认 | 还必须 `allowed === true` 才能使用 |
| Requires Confirmation / 待确认 | 不使用 |
| Missing / 缺失 | 不使用；缺必要定价事实时阻断定价 |
| Rejected / 已拒绝 | 不使用 |

人工补值流程：

**缺包装重量 → Add Value → 输入 0.42 kg → Save Value → 仍待确认、仍阻断 → Confirm → 来源变为人工确认 → Pricing Ready。**

- 来源记录为 `Manual confirmation`，保存 previousValue、previousSource、updatedAt、confirmedAt 和字段 revision。
- 原始供应商目录保留，人工结果按 SKU 保存在 `factEdits`；有效事实驱动资料视图、推荐、定价和文案。
- 重量、长宽高必须大于 0；容量为正整数；空值不能 Confirm。
- 编辑、确认、拒绝会清除当前商品两个平台的旧文案、审核和发布结果。
- 重量、尺寸、采购成本参与定价确认条件和快照版本变化。
- 文案记录事实版本，发布再次检查当前版本是否匹配。
- 待确认或拒绝的杯盖、工艺、吸管等不会被正常模板默认补回。
- 重复、疑似重复和类目不符的商品不能借“核对事实”入口绕过上新限制。

两条已验证的补重量结果：

| 商品 | 成本 | 确认后的模拟建议价 |
| --- | ---: | ---: |
| 内置缺重量 `LM-KT-BTL-005-BLK-500` | USD 8.20 | USD 19.20 |
| 导入缺重量 `LM-KT-IMP-005-BLK-500` | USD 7.90 | USD 18.90 |

两者均加上演示运费 3.10、关税 0.70、平台费 2.20 和目标利润 5.00。主角样例的建议价为 19.99。重量是必要输入门槛，但目前不会据此查询真实物流报价。

人工确认记录操作人员的决定，不会凭空产生检测证据，也不是实名审计或产品性能认证。

## 7. 最新一轮比赛展示优化

本轮保持业务规则不变，重点降低现场讲解成本：

- 复用原五步进度条，增加当前步数和一条下一步提示，没有增加第二套流程导航。
- 任务、SKU、主要动作更容易持续看到；工作区切换回到顶部，弹窗关闭后正确返回焦点。
- Top 1 的选择按钮前移；中英文 1440×900 下均完整位于首屏。
- 分析后折叠长 V1 / V2 详情，保留展开入口，并提供跳到 Fact Review 的次级按钮。
- 缺失、待确认事实优先显示，正在操作的行保持前置。
- 明确区分 Confirmed 和 Allowed，强调确认后的成本和重量仍只用于内部。
- 人工确认后持续显示绿色成功反馈：确认值、Manual confirmation 和当前建议价。
- 红色表示高风险 / 发布阻断，黄色表示修订后待审核，绿色表示通过后可发布。
- 修订和重审按钮前置，小屏幕优先显示风险审核区。
- 文案来源入口移动到标题下方；手机事实表以逐条卡片呈现。
- 原始内部状态名只保留在代码里，界面显示“需要审核”“发布已阻断”等友好文案。
- 顶部新增轻量“演示能力说明”，统一解释 REAL / LOCAL 和 DEMO / MOCK。

英文 1440×900 的实际变化：Top 1 按钮从约 1111px 提前到 841px；Fact Review 从约 1582px 提前到 867px；主要修订按钮约在 591px。桌面和手机均无整体页面横向溢出。Materials 在手机上保留表格横向查看和商品详情入口，不代表所有列同时塞进 375px。

## 8. Real / Local 与 Mock 对照

| 本地真实执行的部分 | 仍使用 Mock / 固定假设的部分 |
| --- | --- |
| XLSX / CSV 读取、固定字段校验 | 任意文件理解、补充 PDF / 图片提取没有实现 |
| 精确 SKU 去重 | 相似商品和语义去重没有实现 |
| 人工编辑、确认、来源保存、状态变化 | 供应商真实性或性能认证没有实现 |
| 依据当前确认事实计算价格 | 运费、关税、平台费用为演示假设 |
| 字段授权、旧结果失效、版本阻断 | 推荐智能和通用语义审核没有实现 |
| 模板文案和实际规则检查 | 没有 LLM 生成或模型效果评估 |
| 实际生成并下载 CSV | Amazon / Shopify 发布为模拟 |
| localStorage 保存与恢复、双语切换 | 无后端、数据库、登录、多用户和公网部署 |

因此，对外可以说“已实现从真实模板文件到有事实权限和审核门槛的上新 Demo”，不能说“已实现 AI 自动核验商品、实时定价并自动上架”。

## 9. 验证和比赛材料

最近已执行并通过：

```bash
npm run typecheck
npm run build
npm run test:e2e
```

仓库没有独立 lint 脚本。最终是 **28 passed（26.9s）**：原有 22 项保留，新增中英文 × 三种屏幕的 6 项展示验收。

覆盖内容包括：

- 原主角、Amazon / Shopify 流程、CSV、R001 阻断、修订后重审。
- 真正上传 XLSX / CSV、去重、坏文件、缺资料、真实字段向下游传递。
- 人工补齐和确认、非法数值、双平台旧结果失效、未确认 / 拒绝事实过滤。
- 刷新恢复、旧版状态兼容、语言切换不改业务状态。
- 1440×900、1920×1080、375×812 下的中英文主线和缺事实支线。
- 能力说明弹窗、主按钮可达性、风险可见性及持续成功反馈。

另外对示例 CSV 与 XLSX 做过独立的 Python CSV / ZIP / XML 内容核对；在用户实际使用的 5173 页面，也按讲稿顺序从补重量一直点击到主角发布和下载。

已交付材料：

| 文件 / 目录 | 内容 |
| --- | --- |
| `docs/DEMO_SCRIPT.md` | 3 分钟、5 分钟讲稿，含现场回退 |
| `docs/DEMO_QA.md` | 20 个问题，覆盖 AI、94 分、证据、确认、平台与后续方向 |
| `docs/DEMO_UX_AUDIT.md` | 审计发现、修复及最终验证记录 |
| `artifacts/presentation/` | 11 张真实 UI PNG，统一 1920×1080，适合 PPT |
| `artifacts/presentation/README.md` | 截图索引和再生成方法 |
| `FACT_REVIEW.md` | 人工确认流程、权限与失效规则 |
| `SUPPLIER_IMPORT.md` | 固定模板、字段定义和导入边界 |

截图覆盖能力说明、资料导入、Top 3、事实核对、缺事实阻断、人工确认解除阻断、文案及来源、风险拦截、修订后待审核、审核通过及发布成功。都来自实际 UI，没有伪造业务状态。

测试通过证明这些受测 Demo 场景可运行，不代表经过真实 AI 效果、平台政策覆盖、大规模并发或完整跨浏览器验收。

## 10. 如何快速演示

### 主线

**导入样例 → 创建任务 → 选择 Top 1 → 分析证据 → 查看事实与权限 → 生成 Amazon 文案 → R001 阻断 → 应用修订 → 再次审核 → 模拟发布 → 下载 CSV。**

### 缺事实支线

**打开缺重量商品详情 → 核对事实 → 输入 0.42 kg → 保存后仍待确认 → Confirm → Pricing Ready。**

3 分钟讲稿把补重量支线放在选主角之前，最后停在模拟发布。进入缺资料 Fact Review 时会自动建立固定任务，补齐后直接点左侧“上新任务”即可，不必再次创建。确认后的另一只水杯可能与主角同分，要按演示固定顺序解释。

内置目录可随时回退；导入后不要立即 Reset，否则会恢复内置数据。导入缺重量样例价格为 18.90，内置缺重量样例为 19.20，讲解时不要混淆。

本地目录：`/mnt/data/pyc/Create_new_products`。Node.js 22.23.2，启动示例：

```bash
cd /mnt/data/pyc/Create_new_products
export PATH=/mnt/data/pyc/.nvm/versions/node/v22.23.2/bin:$PATH
npm run dev -- --port 5173
```

核对时 `http://127.0.0.1:5173` 正常返回 HTTP 200。网页版 GPT 无法访问另一台机器的 localhost；要评价视觉，请同时上传截图。

## 11. 尚未完成和下一步讨论边界

尚未实现：真实 LLM、OCR / PDF / 图片理解、通用语义审核、真实物流 / 汇率 / 税则 / 平台费率、真实平台发布、任意模板导入、复杂任务管理、后端数据库、登录、多用户及生产部署。

当前比赛版本已经收尾，本轮没有继续主动新增这些能力，也不需要为了展示重新搭架构。

希望网页版 GPT 帮助评估：

1. 现有比赛故事是否清楚表达“选品有理由、事实有来源、人工可介入、发布有门槛”？
2. 哪些讲法仍可能让评委把 Mock 误认为真实 AI？
3. 3 分钟和 5 分钟展示应分别保留哪些画面，哪些内容适合留到问答？
4. 如果只做最后的比赛准备，还缺哪些非开发材料，而不是继续堆业务模块？
5. 如果以后转向真实试用，应该先验证哪一个实际能力、用什么数据和验收标准？

请基于上述现状评价，不要重复规划已完成的导入、人工 Fact Review 或五个工作区，也不要默认真实模型与平台已经接通。
