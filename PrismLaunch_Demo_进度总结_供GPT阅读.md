# PrismLaunch Web Demo 当前进度总结

更新时间：2026-09-07（已更新至固定模板 Excel / CSV 导入版本）

本文用于向没有参与开发的网页版 GPT 交接项目进度。内容依据当前源码、运行中的本地页面、构建产物及最近一次浏览器验收记录整理。请将“已实现的前端流程”和“尚未接入的真实业务能力”分开判断。

## 1. 当前做到什么程度

**已完成可从真实固定模板 Excel / CSV 文件开始、一直点击到模拟发布成功的双语 Web Demo。原 P0 / P1 主链路保留，新增供应商文件解析、字段校验、逐行导入报告、SKU 去重和内置数据回退。**

用户现在可以实际操作：查看商品资料、创建固定上新任务、选择推荐商品、生成事实卡 V2、查看建议售价、生成商品文案、触发风险拦截、修订、重新审核、模拟发布和下载 CSV。

现阶段适合演示产品工作流程、界面交互和几个关键约束。已能处理符合固定模板的真实供应商 Excel / CSV，但不支持任意文件、PDF 或图片解析，不能向真实平台上架，也没有验证 AI 选品或语义审核效果。

| 维度 | 当前状态 |
| --- | --- |
| 前端项目 | 已能启动和构建，不是只有静态页面或空框架 |
| 核心流程 | 五个工作区连通，Amazon 与 Shopify 分支均能完成模拟发布 |
| 界面语言 | 中文 / English 即时切换，桌面和手机均可操作 |
| 数据与智能能力 | 保留 10 个内置 SKU，也支持真实 Excel / CSV 商品数据；任务、推荐、补充证据、文案及审核仍使用 Mock 规则和模板 |
| 数据保存 | 当前浏览器 localStorage，可刷新恢复、一键重置 |
| 验证 | 原 8 项验收已在阶段 1 重跑通过；新增导入验收及最终结果见 artifacts/IMPORT_VERIFICATION.md |
| 交付位置 | 本地仓库 `/mnt/data/pyc/Create_new_products`；GitHub 仓库现名为 `gnlhazs111222/Projects`（原名 `Create_new_products`），代码同步至 `pyc` 分支 |

不能据此给出“生产系统已完成多少百分比”。目前完成的是约定范围内的演示版本。

## 2. 项目要表达的业务故事

PrismLaunch（棱镜上新）面向跨境电商商品上新，演示如何把供应商资料变成有依据、经审核的商品内容。

```text
内置供应商资料 / 真实固定模板 Excel、CSV 文件
  → 前端解析与逐行校验（导入时）
  → 资料完整度、重复状态、类目筛选
  → 候选池
  → 创建上新任务
  → Top 3 推荐与匹配理由
  → 选择具体 SKU
  → Evidence / FactCard V1
  → 分析证据，生成任务专属 FactCard V2
  → Pricing Snapshot / 建议售价
  → Amazon 或 Shopify 商品文案
  → 审核发现风险，拦截发布
  → 人工应用修订
  → 重新审核
  → Mock Publish / CSV 导出
```

三个术语：

- **SKU**：一种具体规格商品的编号，例如“黑色、500ml、不锈钢水杯”。换颜色或容量，通常就是另一个 SKU。
- **Evidence**：支持商品信息的资料，例如供应商表格、规格书、商品或包装图片。
- **FactCard**：整理后的商品事实，包含字段值、来源位置、确认状态，以及是否允许用于消费者文案。

## 3. 已实现的界面与功能

### 3.1 全局界面

- 左侧五个工作区：商品资料、上新任务、证据与事实、文案工作室、审核与发布。
- 顶部显示演示数据、模拟智能标识，以及当前任务编号、选中 SKU 和状态。
- 页面上方显示流程进度，可返回前面的工作区。
- 主要操作包含加载反馈、成功或错误通知、禁用状态；前置条件不满足时展示空状态和下一步入口。
- “重置演示”清除业务进度，恢复初始资料页。
- 浅色 B2B SaaS 风格；商品插图和图标使用本地 SVG，不依赖外部图片服务。

### 3.2 商品资料 / Materials

- 展示 10 个 SKU，包含商品名、SKU、类目、颜色、容量、资料状态和重复状态。
- 支持按中英文商品名、类目、颜色及 SKU 搜索。
- 支持全部资料、候选池、暂不参与推荐三种筛选。
- 点击商品可打开详情，查看基础事实、来源、包装信息和定价结果。
- 首页显示 10 个商品、4 个候选、6 个被排除商品。
- 缺包装重量的商品展示“定价已阻断”，不提供建议售价；该商品无法通过详情页直接进入上新选择。

- Materials 新增“导入供应商文件”“载入内置数据”和样例下载入口。
- 严格支持 14 个固定列名，预览有效、缺资料、重复、无效行；默认替换目录，也可仅追加新 SKU。
- 样例 Excel / CSV 包含相同的 8 行商品：6 个 Ready、1 个 Missing Data、1 个 Duplicate；导入后目录为 7 个商品。
- 导入的缺包装重量商品为 `LM-KT-IMP-005-BLK-500`；可以保存和查看，但无法定价或进入推荐。

**边界：**导入实现的是固定模板解析和精确 SKU 去重，不是任意表格理解、语义字段识别或相似商品检测。内置目录的重复标签仍是预置的。文件仅在本地浏览器处理，无后端上传。完整字段规则见 `SUPPLIER_IMPORT.md`。

### 3.3 上新任务 / Launch Tasks

- “创建演示任务”生成固定任务 `PL-DEMO-001`。
- 展示选品要求和最低单位利润。
- 在符合条件的 4 个候选中按固定规则排序，展示 Top 3。
- 每个推荐都有匹配原因及扣分说明；选择后进入证据工作区。

**边界：**目前只有一个固定任务模板，尚无自由填写任务需求、多任务管理或真实 AI 推荐。

### 3.4 证据与事实 / Evidence & Facts

- 展示三类模拟资料：供应商表格、规格 PDF、商品 / 包装图片。
- 可打开证据预览，查看预置的提取结果。
- V1 包含 8 个基础字段：颜色、容量、材质、吸管、原产国、包装重量、采购成本、申报价值。
- 点击“分析证据”，约 700ms 后生成 V2；V2 复制并保留 V1 内容，再补充 4 个字段。
- V2 新增：包装内含、表面工艺、杯盖类型，以及待确认的防漏性能宣称。
- 显示 Confirmed / Requires Confirmation、Listing Allowed / Not Allowed。
- 字段来源包含资料名称和示例位置，例如表格列、PDF 页码、图片区域说明。

**关键示例：**规格书支持“旋拧式杯盖”，可以用于文案；`100% leakproof` 没有检测证据，被标为待确认且禁止用于文案。

**边界：**导入商品的 V1 值来自真实文件，携带实际文件名、工作表和行号；解析不等于独立验证供应商真实性。“分析证据”补充 V2 的行为仍为 Mock，PDF / 图片来源也标明 Mock。没有 OCR、多模态分析或证据冲突判断。V1/V2 仍是当前任务中的对象，不是完整的数据库版本管理系统。

### 3.5 定价 / Pricing

主角 SKU 使用固定的 Demo Pricing Snapshot v1：

| 项目 | 金额 |
| --- | ---: |
| 采购成本 | USD 8.20 |
| 运费 | USD 3.10 |
| 预估关税 | USD 0.70 |
| 平台费用 | USD 2.20 |
| 成本合计 | USD 14.20 |
| 目标最低利润 | USD 5.00 |
| 建议售价 | USD 19.99 |
| 此售价对应的模拟单位利润 | USD 5.79 |

- 缺少包装重量或包装尺寸时，返回 blocked 状态，建议价为空。
- 详情页和文案工作室均有相应定价展示。
- 采购成本、申报价值、运费、关税不会被写进消费者文案。

**边界：**没有实时物流、汇率、税则或平台费率查询；USD 19.99 是内置和样例主角的演示售价；导入成本较高时，最低价格会随成本变化，以满足模拟目标利润，仍不代表真实商业报价。

### 3.6 文案工作室 / Listing Studio

- 支持 Amazon US 和 Shopify US 两个 Tab。
- 从 V2 中 `Confirmed` 且允许用于文案的字段，按模板生成标题、卖点、描述和属性。
- 可展开“事实来源”，查看用于生成的字段及来源。
- 可手动编辑标题、卖点和描述，保存为新版本。
- 可根据事实重新生成文案；重新生成后需要重新审核。
- 两个平台分别保存文案、审核结果及发布结果。

**刻意保留的演示例外：**首次生成 Amazon 草稿时，会额外注入 `100% leakproof`，用于展示后续风险拦截。它不是通过字段允许列表正常生成的事实。

**边界：**文案由确定性模板生成，没有调用 LLM。两个平台目前共享大部分基础内容结构，只做演示层面的展示差异，尚未实现完整的平台专属字段体系和约束。

### 3.7 审核与发布 / Review & Publish

- Run Review 检查当前文案，展示问题编号、风险级别、原文和原因。
- **R001**：命中 `100% leakproof`，显示无证据支持的性能宣称，并禁用 Publish。
- “应用建议修订”恢复有依据的文案，其中杯盖描述为：

  > Secure screw-top lid designed for everyday carrying.

- 修订后不会自动获得通过结果，必须重新 Run Review。
- 审核通过后显示“高风险问题：0”，才启用发布。
- **R002**：其他与已确认模板不同的改写，被判为待确认内容。
- 编辑文案会增加版本号，清除该平台旧审核和发布结果，防止沿用旧版本的通过状态。
- 模拟发布动作还会在本地数据层重新检查当前版本是否满足发布条件。
- Amazon 发布完成后可真实下载 CSV，当前为 1 条商品记录、12 列。
- Shopify 发布完成后显示 `SHOP-DEMO-1042 / Draft`；Amazon 显示 `AMZ-DEMO-1042 / Export ready`。

**边界：**审核规则主要是预置风险匹配和与确认模板比较，不具备通用语义审核或合规认证能力。“应用建议修订”会恢复整份安全模板，不只是替换某一个短语。发布 ID 和结果固定模拟；没有真实 Shopify 商品或 Amazon 上架记录。CSV 是演示格式，不是经过官方验证的上传模板。

## 4. 当前演示数据

主角商品：

```text
SKU: LM-KT-BTL-001-BLK-500
商品: Black Stainless Steel Travel Bottle / 黑色不锈钢随行杯
颜色: Black
容量: 500ml / 16.9 fl oz
材质: Stainless Steel
吸管: false
原产国: China
包装重量: 0.38 kg
包装尺寸: 8 × 8 × 25 cm
资料状态: search_ready
重复状态: unique
```

10 个 SKU 的构成：

| 类型 | 数量 | 用途 |
| --- | ---: | --- |
| 完全符合演示需求的主角 | 1 | 稳定 Top 1，94 分 |
| 相似但条件不同 | 3 | 有吸管、750ml、象牙白，分别展示扣分因素 |
| 资料缺失 | 2 | 缺包装重量；缺包装尺寸及配件信息 |
| 完全重复 | 1 | 展示推荐前排除 |
| 疑似重复 | 1 | 展示需人工核实、暂不推荐 |
| 类目不符 | 2 | 帆布包、台灯，排除出水杯选品任务 |

固定任务：Amazon US / United States / Home & Kitchen，要求黑色、简约通勤风格、约 16 盎司、无吸管、优先包装与配件信息完整，最低单位利润 USD 5。

排序从 94 分起，有吸管扣 12 分，容量过大扣 16 分，颜色不符扣 23 分。因此 Top 3 为 94 / 82 / 78 分。这些分数用于解释流程，不是模型置信度或效果评估结果。

## 5. 双语与状态管理

- 右上角“中文 / EN”即时切换，五个工作区的主要界面内容均已接入翻译字典。
- 首次跟随浏览器语言，之后记住用户选择。
- 业务状态键为 `prismlaunch.demo.v1`，语言偏好键为 `prismlaunch.language`。
- 切换语言不改变任务、SKU、事实卡、文案版本、审核或发布结果。
- Reset Demo 清除业务状态，但保留语言偏好。
- 美国市场的商品文案、编辑内容和 CSV 始终保持英文；中文界面明确说明这一点。
- 刷新恢复当前进度；浏览器存储不可用时使用内存并提示刷新会丢失进度。
- 成功替换或追加导入会重置旧上新进度；取消、无效模板或零个有效新商品不会改变当前业务状态。载入内置数据和 Reset 都恢复原 10 个 SKU。
- 旧版 localStorage 会话会补入内置 catalog，保留已有任务；导入目录和报告可以刷新恢复。
- 切换到另一个 SKU，会清除原 SKU 对应的事实扩展、定价、文案、审核和发布结果；不支持并行保存多个商品的完整任务历史。

支持以下 10 个状态名；用户返回、编辑或切换平台时，状态可随相应业务结果变化，并非只能单向前进：

```text
initial → materials_ready → task_created → sku_selected
→ evidence_analyzed → pricing_ready → listing_generated
→ review_blocked → review_passed → published
```

## 6. 技术实现与本地运行

技术栈为 React + Vite + TypeScript + Tailwind CSS，使用 Radix Dialog / Tabs 和 Lucide 图标。没有新增复杂后端、数据库或 Agent 执行框架。

```text
src/
  App.tsx                 全局布局与操作协调
  components/             通用控件、定价、文案预览、业务上下文
  pages/                  五个工作区
  data/mockData.ts         10 个 SKU、固定任务
  services/mockApi.ts      统一数据访问、导入预览/提交、模拟规则和状态保存
  services/supplierImport.ts 固定模板文件解析、校验和精确 SKU 去重
  types/index.ts          商品、事实卡、定价、文案、审核等数据类型
  i18n/                   双语字典、语言偏好与切换组件
tests/
  demo.spec.ts            原始业务链路验收
  language.spec.ts        双语与语言切换验收
  import.spec.ts          Excel/CSV 上传、主链路、追加、坏文件及数据传递验收
artifacts/                截图、CSV 样例、验收记录
```

UI 通过 mockApi 使用业务数据。Promise 和短延迟用于模拟请求，全部业务仍在浏览器中执行。原仓库项目书、流程图和资源保留。

本地路径：`/mnt/data/pyc/Create_new_products`。Node.js 使用 22.23.2，依赖已安装，有 `package-lock.json` 和 `.nvmrc`。

```bash
cd /mnt/data/pyc/Create_new_products
export PATH=/mnt/data/pyc/.nvm/versions/node/v22.23.2/bin:$PATH
npm run dev -- --port 5173
```

文档核对时，本地 `http://127.0.0.1:5173` 返回 HTTP 200。它不是公网部署地址，网页版 GPT 无法通过自己的 localhost 访问此 Demo。

## 7. 已经实际做过的验证

以下为初版八项覆盖范围。新增导入版本先完成了阶段 1 的八项回归（8 passed，24.9s），再增加导入测试；最终构建、测试和变更清单见 `artifacts/IMPORT_VERIFICATION.md`。

开发阶段已执行依赖安装、TypeScript 检查、生产构建和浏览器操作验证。最近一次双语版本测试结果为 **8 passed（24.5s）**。

| 验收项 | 关键覆盖 |
| --- | --- |
| 1. Amazon 全链路 | 重置、推荐、V1/V2、定价、文案、R001、修订重审、发布、CSV、刷新与状态节点 |
| 2. 资料缺失和重复排除 | 缺包装重量不显示建议价；4 个候选、6 个排除；重复与疑似重复不进入推荐 |
| 3. Shopify 独立分支 | Amazon 仍被拦截时，Shopify 可单独生成、审核、模拟发布；切回 Amazon 仍阻断 |
| 4. 文案修改与旧审核失效 | 审核通过后改文案，旧结果失效；未确认内容触发 R002；修订重审后再开放发布 |
| 5. 英文手机流程及恢复 | 375px 完整操作、无页面横向溢出、证据弹窗 Escape 关闭、损坏存储恢复 |
| 6. 中文桌面流程 | 1440px 中文 Amazon / Shopify 发布，中途切换语言，业务状态保持一致 |
| 7. 中文手机流程 | 375px 中文完整链路、语言切换及英文 CSV 内容验证 |
| 8. 语言偏好 | 用户选择覆盖浏览器默认语言，刷新和重置保留偏好 |

测试由 Playwright 启动生产构建预览，再通过 Chromium 点击真实 UI。另用 Python CSV 解析器检查过下载文件的 SKU、售价和列结构。

初版进度总结只核对已有记录；本轮继续开发按要求先重跑初版测试，再对真实文件导入执行新增测试和全量回归。

**验证范围：**这些结果说明上述固定数据下的界面流程已通过检查，不说明真实识别、推荐、合规审核或真实平台发布已经验证。没有进行大规模数据、并发、多用户或跨浏览器兼容性验收。

## 8. 如何向别人演示

新增入口：Materials 下载样例 → 导入供应商文件 → 默认替换 → 校验摘要 8 / 6 / 1 / 1 / 0 → 导入 7 个商品 → 继续下方原主链路。样例位于 `public/demo/prismlaunch-supplier-demo.xlsx` 和 `.csv`。导入后不要先点击重置，否则会恢复内置目录。

建议先切中文，再按下面顺序点击，约 3～5 分钟：

1. **重置演示**，展示 10 个商品以及 4 个候选、6 个排除。
2. **创建演示任务**，说明需求和 Top 3 理由；选择第一名。
3. **分析证据**，展示 V1 保留、V2 补充，以及允许 / 禁止使用的字段。
4. 指出建议售价 **USD 19.99**，进入文案工作室。
5. **生成 Amazon 文案**，展开事实来源，留意预置的 `100% leakproof`。
6. **继续前往审核 → 运行审核**，展示 R001 与无法点击的发布按钮。
7. **应用建议修订 → 再次运行审核**，展示高风险问题变为 0。
8. **发布 → 导出 Amazon CSV**，完成主链路。
9. 可选：切 Shopify 生成并发布草稿；或回到资料页查看缺包装重量的 `LM-KT-BTL-005-BLK-500` 如何被阻断。

可搭配上传给网页版 GPT 的截图：

- `artifacts/materials-zh-1440.png`：中文商品资料页。
- `artifacts/review-zh-1440.png`：中文风险拦截页。
- `artifacts/listing-studio.png`：英文文案和事实来源。
- `artifacts/publish-success.png`：发布成功结果。

单独上传本 Markdown 可理解实现范围；要评价视觉效果，需要同时上传截图。

## 9. 尚未实现与后续决策

以下均为待办候选，不是已实现功能，也不是已经承诺的开发排期：

| 方向 | 尚缺内容 |
| --- | --- |
| 更多资料接入 | 非固定模板表格、PDF、OCR、图片理解；固定模板 XLSX / CSV 导入已实现 |
| 商品事实管理 | 真实字段对齐、证据冲突、人工确认 / 补录、持久化版本历史 |
| 选品 | 可编辑任务、多任务、真实检索或推荐算法、推荐效果评估 |
| 定价 | 真实运费 / 税费 / 汇率 / 费率输入、可配置假设、快照追溯 |
| 文案 | LLM 生成、平台专属结构与约束、真实来源引用检查 |
| 审核 | 通用语义核查、平台规则库、支持任意合理改写的审核、误报漏报评估 |
| 发布 | Shopify / Amazon API、真实认证、官方模板、错误重试和幂等 |
| 工程交付 | 部署方案、后端数据库、账号与多用户协作、运行监控 |

如果接下来仍以比赛演示为目标，可先补讲解素材、页面说明和评委常问问题；如果要做真实试用，可在已完成的“固定模板文件 → 字段 → 文件与行号”基础上补人工确认、真实 PDF / 图片证据，再逐步替换后续 Mock。不要一次性把所有服务接入当作下一步的唯一方案。

## 10. 希望网页版 GPT 帮助判断什么

请基于以上已实现状态，帮助评估：

1. 这个 Demo 是否已清楚表达“选品有理由、文案有依据、风险可拦截”的产品价值？
2. 哪些环节目前只能证明 UI 跑通，哪些还需要真实数据或独立评估？
3. 在时间紧张的条件下，为比赛演示补哪三件事最有价值？
4. 如果转向真实试用，下一阶段最小闭环应该是什么，怎样定义验收标准？
5. 对外介绍时，哪些表达容易把 Mock 夸大成已落地能力，应如何改写？

请不要默认项目已经具备真实 AI 分析、语义审核、平台上架或生产部署能力；也不需要从零重新规划已经完成的五个工作区。
